{
  "name": "envoyage-consumer-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal example: consume envoyage from a TS product via the @envoyage/browser SDK — drive a browser, watch it live, and hand off to a human.",
  "scripts": {
    "driver": "tsx driver.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@envoyage/browser": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
