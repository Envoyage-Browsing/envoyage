{
  "name": "ringtail-consumer-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal example: consume rudder from a TS product — drive a browser via MCP and render frames + a (placeholder Rocco) mascot cursor.",
  "scripts": {
    "driver": "tsx driver.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ai": "^4.0.0",
    "@ai-sdk/google": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
