meta {
  name: Crawl — cancel
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/crawls/{{crawlId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: application/json
}
