meta {
  name: Crawl — download asset
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/crawls/{{crawlId}}/assets/{{crawlAssetId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Accept: image/*
}
